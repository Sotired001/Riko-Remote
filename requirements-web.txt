# Requirements for Riko Orchestrator Web UI
flask>=2.3.0
flask-socketio>=5.3.0
requests>=2.31.0
pillow>=10.0.0

# AI Assistant dependencies
# Note: Ollama must be installed separately (see install_ai.bat)

# Optional: for better production serving
gunicorn>=21.0.0
eventlet>=0.33.0