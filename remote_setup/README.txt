DEPRECATED: This `remote_setup/` folder and its files have been superseded by `agent_setup/`.

DEPRECATED: legacy setup folder. Use `agent_setup/` for the canonical agent files.

This folder is retained only for historical/backward-compatibility reasons.
Prefer `agent_setup/` for any new installations or references.